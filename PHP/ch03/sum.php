<?php
    $n1 = 70;   //70 - 리터럴(Literal) : 본연의 값 이라고 한다.
    $n2 = 40;
    $sum = $n1 + $n2;
    //참고)상수(한번대입된 값이 안바뀌는 값)와 리터럴(본연의 값)은 다른 개념이다!
    //PHP의 상수는 딱한번만 가능

    print "<div>" . $sum . "</div>";

    $sub = 12.3 - 42.72;
    
    print $sub;


?>